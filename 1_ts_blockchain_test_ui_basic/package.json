{
  "name": "TypeScript_GONT Blockchain UI Test",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "start": "serve",
    "compileDeploy": "tsc && npm run deploy",
    "deploy": "copyfiles -f src/browser/*.html src/browser/*.css dist"
  },
  "devDependencies": {
    "@types/node": "^10.14.21",
    "copyfiles": "^2.1.1",
    "serve": "^13.0.2",
    "typescript": "~3.0.0"
  }
}
